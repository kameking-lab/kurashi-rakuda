# 13. プロジェクト全方向診断（2026-07-19）

- 診断実施: 診断専任セッション（wo-fable）。修正コミットなし（本ドキュメントと BACKLOG 起票のみ）
- 診断基準 SHA: `957790ac431b0f0a929d014397509d8e5fa1dbcd`（診断開始時の origin/master = ローカル HEAD、一致確認済み）
- 方針: すべて実測・実ログ・実 HTML に基づく。並行セッションの過去報告は引用せず再実測した
- 実測環境: 本番 https://kurashi-rakuda.vercel.app への Playwright 実機巡回（デスクトップ 1280x800／iPhone13 相当 390x844）、Lighthouse CLI（mobile・simulated throttling）、フレッシュビルド（.next 削除後 `npm run build`）、`npm run check` 全ゲート、`node scripts/verify-seido.mjs --fetch`、本番 HTML/ヘッダの curl 実測、リポジトリ全履歴（439 コミット）の機微情報スキャン

## 0. エグゼクティブサマリ

サイトの中身（計算の正確性・出典規律・ブランド哲学・プライバシー）は極めて高水準で、YMYL 独立検算 3 本・機微情報スキャン・広告要素スキャンはすべて問題ゼロだった。一方で「作ったものが届いていない・見つからない・重い」という配送側の問題が集中的に見つかった。S 級は 3 件: ①本番が 12 コミット前のまま（保育料 48 自治体が未反映）、②ツール詳細ページの First Load JS 1.03MB（本番 LCP 5.6〜5.7 秒、CI バジェット 2.5 秒の倍超）、③記事内リンク切れ 19 箇所。いずれも修正手段は明確で、次波で解消可能。

## 1. 所見一覧

書式: 症状 / 根拠 / 重大度（S=即対応・A=次波・B=改善・C=任意）/ 推奨担当 / 依存 / 並列可否

### S 級（即対応）

#### S-1 デプロイドリフト: 本番が master より 12 コミット古い（保育料 48 自治体が未反映）
- 症状: 本番の保育料ツールに収録されている自治体は 99。リポジトリ master は 147。#228〜#239（東日本一般市バッチ 1〜6 = 48 市 + queue docs）が本番に反映されていない。
- 根拠: 本番 `/tools/childcare/hoikuryo` の SSG HTML（2026-07-19 取得、290,109 bytes）にバッチ 1〜6 の全市（所沢・松戸・上尾・安城・西尾・弘前ほか）が 0 件、#225 までの市（福井 19 件・明石 9 件・豊橋 19 件・高槻 21 件）は存在。`git ls-tree` で 8867e4b(#225) 時点の data/seido/hoikuryo = 99 ファイル、957790a = 147 ファイル。#228 のマージは 2026-07-18 21:07 JST、本番エッジキャッシュの Age は約 23.5 時間。CI は #228 以降も全 green（ビルド失敗ではない）。
- 含意: Vercel の自動デプロイが止まっているか、Git 連携が切れているか、デプロイ失敗が通知されずに握られている。リポジトリ側からは原因を特定できない（Vercel ダッシュボード権限が必要 → 社長確認事項 D-1）。
- 重大度: **S** / 推奨担当: フェイブル5（原因特定と再発防止設計。ダッシュボード確認は社長）/ 依存: D-1 / 並列可

#### S-2 ツール詳細ページの巨大バンドル: First Load JS 1.03MB・本番 LCP 5.6〜5.7 秒
- 症状: どのツールページを開いても全 64 ツールのコード + 保育料 147 自治体の階層表 JSON（約 3.2MB 分のデータ）がクライアントに配信される。本番 Lighthouse(mobile) 実測: sankyu-ikukyu-money LCP 5.7s / Perf 70、hoikuryo LCP 5.6s / Perf 77。CI バジェット（LCP ≤ 2500ms・Perf ≥ 90）を本番実態が大幅超過。
- 根拠: フレッシュビルドで `/tools/[category]/[slug]` の page チャンク 4,058,353 bytes（gzip 後 918kB、First Load 1.03MB）。`app/(site)/tools/[category]/[slug]/page.tsx`（2,449 行）が import 文 120 本で全ツールコンポーネントを静的 import、`next/dynamic` 不使用。チャンク内に「世田谷」「弘前」等の自治体データ実体を grep で確認。トップ/記事は Perf 98〜99・LCP 1.6〜2.3s と健全（差分がバンドル起因である証拠）。
- 含意: 保育料自治体を増やすほど（目標は全国 1,700 超）全ツールページが際限なく重くなる。現行の量産方針と正面衝突するスケーリング欠陥。CI が検出できないのは LHCI の計測対象が最軽量ツール 1 本のみのため（B-9 参照）。
- 重大度: **S** / 推奨担当: オーパス4.8（slug ごとの dynamic import 化・formula 解説の分離・保育料データの遅延ロード）/ 依存: なし / 並列可（page.tsx を触る他系統と要調整）

#### S-3 記事内リンク切れ 19 箇所（12 記事）: 関連導線が死んでいる
- 症状: 記事本文の内部リンクのうち 19 箇所が 404。(1) `/articles/<slug>` 形式 15 箇所（正: `/guide/<category>/<slug>`。リンク先記事はすべて実在）、(2) `/tools/career/part-shift-shunyuu-keisan` 4 箇所（正カテゴリは money。正 URL の 200 は実測確認済み）。
- 根拠: 本番をトップから幅優先で全クロール（総数 199 URL、200=184 / 404=15 / 500=0）。発生源は `content/articles/` の 13+4 ファイル（seido-juuminzei-shikumi-2026.md、seido-kyuyo-shotoku-koujo-2026.md、seido-koyou-hoken-ryoritsu-2026.md、seido-kyoukaikenpo-hokenryo-2026.md ほか）。
- 重大度: **S**（機械置換で即修正可能・実害は UX/SEO 双方）/ 推奨担当: ソネット5 / 依存: なし / 並列可

### A 級（次波）

#### A-1 8/1 改定の新額差し替え（D1 実行）が唯一の残タスク
- 症状: フェイルセーフ自体は完備（後述 §5）だが、新リーフレット公表後 7/31 までに差し替えないと 8/1 から 5 ツール（産休育休・時短・復職手取り・介護休業・産後手続き）が停止表示になり、verify-seido が expiresOn 超過をエラー化して CI が恒常赤になる。
- 根拠: `data/seido/ikukyu-kyufu.json` amendments expiresOn=2026-07-31、`data/seido/kaigo-kyugyou-kyufukin.json` 同。verify-seido.mjs L675-680。実行台本は docs/10 §1.5-1.6 に整備済み。
- 重大度: **A** / 担当: オーパス4.8（差し替え）+ フェイブル5（独立検算。docs/10 の役割分担どおり）/ 依存: 厚労省リーフレット公表 / 直列（8/1 前後の最優先ジョブ）

#### A-2 GSC 未登録・インデックス状況が観測不能
- 症状: リポジトリ・本番 HTML のどこにも google-site-verification が存在しない。site: 検索でも当ドメインのページを確認できず（検索経路の精度限界あり）。robots/sitemap/canonical/noindex は全て健全なので、未インデックスなら原因は「新規サイト + 被リンクゼロ + GSC 未登録」側。
- 根拠: 全リポ grep 0 件、主要 12 ページの実 HTML 検査、robots.txt/sitemap.xml(184 URL) 実取得。
- 重大度: **A** / 担当: 社長（GSC 登録は所有者権限）+ ソネット5（verification meta 追加と sitemap 送信手順化）/ 依存: D-2 / 並列可

#### A-3 パーソナライズパネルが初回タップで空のまま開く（ハイドレーション競合）
- 症状: ページ表示直後（load 後約 1 秒）に「あなた向けに並べ替える」をタップすると、枠だけ開いて中身が 0 要素。5 秒後の操作では正常（10 コントロール出現）。初見ユーザーが最初に触る位置で「壊れてる」と映る。
- 根拠: Playwright 実機で再現（スクリーンショット int-pers2-opened2.png、DOM 実測 summary 以外の子要素 0）。`DeferredPersonalization.tsx` が LCP 後 2.2 秒で lazy ロードする設計との競合。
- 重大度: **A** / 担当: オーパス4.8 / 依存: なし / 並列可

#### A-4 パーソナライズ UI に文字化け「姊活・姊娠」（正: 妊活・妊娠）
- 症状: ライフイベント選択チェックの表示ラベルが文字化けしたまま本番公開されている。
- 根拠: `components/personalize/DeferredPersonalization.tsx:37`。
- 重大度: **A**（1 行修正・信頼毀損が大きい）/ 担当: ソネット5 / 依存: なし / 並列可

#### A-5 検索: Enter 無反応・/tools に検索なし
- 症状: トップの検索欄はインクリメンタル候補のみで、Enter（スマホの「検索」キー）を押しても何も起きない。64 本が並ぶ /tools には検索入力が 1 つもない。
- 根拠: Playwright 実測（int-search-enter.png、URL 変化なし。/tools の input 要素 0 件）。
- 重大度: **A** / 担当: 仕様=フェイブル5（A-6 と一体で探索性の情報設計）、実装=オーパス4.8 / 依存: なし / 並列可

#### A-6 /tools 一覧の探索性不足: パーソナライズが一覧に効かない・縦 1.1 万 px の壁
- 症状: パーソナライズ保存後もトップの推薦 6 枠が変わるだけで、/tools の並び・絞り込みには一切反映されない（保存前後で先頭 12 件完全一致を実測）。一覧はカテゴリ見出しだけの単調な縦 1 カラム約 11,000px。ライフステージ chips は上部固定されず下までスクロールすると使えない。「カテゴリ分かりづらい問題」は部分的解消に留まる（判定: 未解消）。
- 根拠: Playwright before/after 実測（pers6-tools-after.png ほか）、localStorage 実測値 `kurashi-rakuda:personalization:v1`。
- 重大度: **A** / 担当: フェイブル5（カテゴリページ復活か一覧フィルタ強化かの方針決定を含む）→ 実装オーパス4.8 / 依存: B-8（カテゴリページ不在）と同根 / 並列可

#### A-7 保育料カードの市名 30 個超の羅列
- 症状: トップ・一覧・ツール本体の説明文で対応自治体名がモバイル 1.5 画面分続く。読む気が失せ、スパム的に安っぽく見える。147 自治体・将来 1,700 に耐えない見せ方。
- 根拠: m-top-s5.png、m-tool-hoikuryo-s0.png 実測。
- 重大度: **A** / 担当: ソネット5（「147 自治体対応・お住まいの市を検索」形式へ、registry description の生成方法変更）/ 依存: なし / 並列可

#### A-8 児童扶養手当ツールの入力ハードル: 「所得額」直接入力・扶養親族初期値 0
- 症状: 年収ではなく課税証明書の「所得金額」を直接入力させる（警告表示はあるが、初見の母親は自分の所得金額を知らないことが多い）。「扶養親族の数」初期値が 0 のため、児童 2 人でもそのまま計算すると実態より厳しい判定（支給停止）が出やすいことを実測で確認。
- 根拠: 本番実入力（ymyl-jidoufuyou-b.png: 児童 2・所得 250 万・扶養 0 のまま → 限度額 208 万で不支給判定。扶養 2 なら一部支給圏 284 万）。計算自体は正確（§4）。
- 重大度: **A**（YMYL ど真ん中の UX）/ 担当: フェイブル5（年収→所得換算補助の仕様。既存 kyuyo-shotoku-koujo データで実装可能かの判断）→ 実装オーパス4.8 / 依存: なし / 並列可

#### A-9 記事の表示崩れ: コードフェンス生表示 5 本・内部データ生値露出
- 症状: (1) 自前 Markdown レンダラーがコードブロック非対応のため、``` を含む 5 記事で本番にバッククォートが生表示。(2) 「自己負担割合 0.3rate を掛けて」と内部データの生値+単位が本文露出（本番表示実測）。(3) 「整備済みデータでは」等の内部用語の読者向け文面への漏出。
- 根拠: tool-narisou-yosan-haibun-keisan.md ほか 4 本（本番実測）、tool-narisou-funin-chiryou-hoken-tekiyou.md:20、seido-kyuyo-shotoku-koujo-2026.md。
- 重大度: **A** / 担当: ソネット5（記事修正）+ オーパス4.8（レンダラーのコードフェンス対応 or lint でフェンス禁止）/ 依存: なし / 並列可

#### A-10 brand-lint が「表現」しか見ておらず、収益タグ・URL を検知できない
- 症状: アフィリエイトドメイン（a8.net/amzn.to/moshimo 等）・広告スクリプト（adsbygoogle/googletagmanager）・sponsored 属性の混入を機械検知する仕組みがない。現時点の混入は全数スキャンでゼロだが、「広告臭ゼロ」の機械的担保に最大の穴。
- 根拠: `.github/scripts/brand-lint.mjs`（11 表現の行単位検査のみ）。content 全 111 記事 + components 152 ファイル + 本番 4 ページの実測スキャンは検出ゼロ。
- 重大度: **A** / 担当: ソネット5（禁止ドメイン/タグリストの追加。P2-I06 と統合可）/ 依存: なし / 並列可

#### A-11 扶養の壁 178 万円の第三者再検算（YMYL 検算プロセスの教訓）
- 症状: 本診断の独立検算で、期待値作成側（令和 7 年分ルール: 160 万/123 万で計算)と実装出力（令和 8 年分: 178 万/136 万）が乖離した。データ側 `fuyou-kabe.json` は令和 8 年度税制改正（財務省大綱「課税最低限を 178 万円」= 給与所得控除 74 万 + 基礎控除 104 万、令和 8・9 年分特例）を機械照合済みで収録しており、実装が正・検算側の見落としと判定した。ただし「検算者ですら最新改正を見落とす」ことが実証されたため、178/136/207 万の各壁は別の検算者による原文再照合を推奨する。
- 根拠: `data/seido/fuyou-kabe.json` L369-390（verify.expect「課税最低限を178」が --fetch 照合 PASS）、本番実入力 3 ケース（ymyl-fuyokabe-*.png）。
- 重大度: **A** / 担当: フェイブル5（財務省大綱・国税庁あらまし原文との独立再照合）/ 依存: なし / 並列可

#### A-12 日付ユーティリティの 15 重実装
- 症状: SimpleDate interface の重複定義 5 箇所、addDays/diffDays が ISO 文字列版・SimpleDate 版・Date 版の 3 流派で約 15 ファイルに散在。うるう年・タイムゾーンのバグ修正が 15 箇所同時修正になるリスク。
- 根拠: Getsurei.calc.ts:8 / HokatsuSchedule.calc.ts:12 / RinyushokuRyo.calc.ts:12 / Yobousesshu.calc.ts:25 / YuukyuuFuyoNissuuKijun.calc.ts:68 ほか（lib/tools/seido.ts:63 に既存 addDays があるのに未使用）。
- 重大度: **A** / 担当: オーパス4.8（lib/tools/date.ts へ統合・全テスト green 維持）/ 依存: なし / 並列可（多数ファイルを触るため他系統の量産と時期調整）

#### A-13 サンプル記事テンプレートが本番公開されている
- 症状: /guide/sample-dandori 等 3 本がダミー文（「手順のタイトル 目安時期」）のまま 200 で公開。noindex 付与・sitemap 非掲載・内部リンクなしは確認済みだが、URL 直撃で「未完成サイト」の印象を与える。
- 根拠: 本番実測（m-guide-dandori-s1.png）、`app/(site)/guide/sample-dandori/page.tsx` L26 robots.index=false。
- 重大度: **A**（公開サイトの品格）/ 担当: ソネット5（本番ビルドから除外 or 開発環境限定化）/ 依存: なし / 並列可

### B 級（改善）

#### B-1 R7 据置の保育料 13 自治体（前年度額を提示中）
- 症状: aichi-nagoya, chiba-chiba, chiba-ichikawa, fukuoka-kurume, hiroshima-fukuyama, hyogo-himeji/itami/kakogawa/takarazuka, kanagawa-hiratsuka/sagamihara, mie-yokkaichi, osaka-kishiwada が fiscalYear=2025 のまま（R8 版未公表のため。under-review 明記・nextCheckDue 9〜10 月前倒し済みで統制下にはある）。
- 重大度: B / 担当: ソネット5（9〜10 月の再訪キュー化）/ 依存: 各市の R8 公表 / 並列可

#### B-2 呉市の保育料が実質空データ
- 症状: `data/seido/hoikuryo/hiroshima-kure.json` の tiers が `{"tier":"未確認","fees":{}}` 1 行のみで金額ゼロ件（R8 前期の階層表を市が未公表・10 月から全額無償化予定という特殊事情）。誤額は出ないが「収集済み」を名乗って答えを返せない。
- 重大度: B / 担当: ソネット5（10 月無償化の予告表示に作り替えるか、収集済みリストから外す）/ 並列可

#### B-3 verify-seido の死角: null 値・「未確認」・空 fees を素通し
- 症状: value:null（実害候補 122 件）・「未確認」文字列（437 箇所）・空 fees（呉市）を検知するゲートがない。under-review 75 件に機械的な再訪期限がない。verify.skip が恒久スキップ可能。
- 根拠: scripts/verify-seido.mjs 構造検査に該当チェックなし（詳細集計は診断時 scratchpad の census-out.json）。
- 重大度: B / 担当: オーパス4.8（ゲート追加は既存 184 ファイルの一斉是正を伴うため設計込み）/ 並列可

#### B-4 CI の穴: check:mascot 欠落・LHCI 対象に重量ページなし
- 症状: (1) `npm run check` に含まれる check:mascot が ci.yml に無く、マニフェスト破損が PR を素通りする。(2) lighthouserc.json の計測対象が最軽量ツール（chomiryo-kanzan）のみで、S-2 のバンデル劣化を CI が観測できない。CLS バジェット 0.05 に対し記事ページ実測 0.08 も未観測。
- 根拠: ci.yml L20-33、lighthouserc.json、本番 Lighthouse 実測（article CLS 0.08）。
- 重大度: B / 担当: オーパス4.8 / 依存: S-2 修正後に LHCI 追加が望ましい（先に追加すると CI 赤化）/ 直列注意

#### B-5 セキュリティヘッダ欠落
- 症状: CSP / X-Frame-Options / X-Content-Type-Options / Referrer-Policy / Permissions-Policy がすべて無い（HSTS は Vercel 既定で有効）。認証・Cookie なしの静的サイトのため実害は限定的だが、next.config.ts の headers() で低コストに追加可能。
- 根拠: 本番レスポンスヘッダ実測（2026-07-19）、next.config.ts に headers() なし。
- 重大度: B / 担当: オーパス4.8 / 並列可

#### B-6 マスコット画像の権利根拠が弱い
- 症状: 10 ポーズは Gemini の Web UI をブラウザ自動操作して生成（mascot/browser-generation.log に経緯が Public で可読）。消費者向け UI の自動操作は Google 利用規約と抵触リスクがあり、商用利用の根拠が API 課金経由より弱い。権利・由来のメタデータ管理ファイルなし。
- 重大度: B / 担当: フェイブル5（方針判断: 現状維持 / API で再生成 / 権利記録整備）→ 社長報告 / 並列可

#### B-7 固定 6 ページの canonical 欠落・og:url/og:type 全ページ欠落
- 症状: /about /policy /contact /privacy /disclaimer /sources に canonical なし（他ページは全てあり）。og:url・og:type は全 12 検査ページで欠落。
- 重大度: B（canonical）/ C（og）/ 担当: ソネット5 / 並列可
- 併記: 404 ページの title がサイト既定のまま（not-found.tsx に metadata 未定義）= C。

#### B-8 カテゴリページ不在（/tools/money 等が 404）
- 症状: カテゴリ導線がすべて `/tools#money` 形式のページ内アンカーで、URL 推測・パンくず遡行で 404。カテゴリ単位の共有・ブックマーク・SEO ランディングが存在しない。sitemap 整合はしている（載せていないので嘘はない）。
- 根拠: 本番実測 /tools/money=404、`app/(site)/tools/[category]/page.tsx` 不存在。
- 重大度: B / 担当: フェイブル5 の A-6 方針判断に統合 / 並列可

#### B-9 モバイル表示の粗さ（第一印象を落とす群）
- 症状: (1) トップ見出し「暮らしの荷物 を、/ひとつ軽く。」の不自然な改行、/guide は「迷った/ときの、/確かな/道しる/べ。」とほぼ 1 文字ずつ縦落ち。(2) 「あなた向けに並べ替える」カードの折返し崩れ・開閉アフォーダンス欠如。(3) トップ「64 公開中の無料ツール」vs /tools ヒーロー「48+ FREE TOOLS」の数字不整合。(4) フッターリンクのタップ領域 14px 高（推奨 44px）。(5) スティッキーヘッダー半透明で下の文字が透けて重なる。(6) ツール詳細がモバイル 28,854px（保育料）と長大で入力→結果の往復が遠い。
- 根拠: すべて Playwright スクリーンショット実測（m-top-s0/s1/s2.png、m-tools-s0.png ほか）。
- 重大度: B / 担当: ソネット5（(1)〜(5) の定型修正）+ オーパス4.8（(6) はレイアウト設計）/ 並列可（UI 格上げ第 2 弾 wo-codex と要調整）

#### B-10 制度型記事テンプレの機械適用による不整合
- 症状: 制度型 50 本すべてが「## 対象と金額」見出し。チャイルドシート記事（金額の話がない）・冷凍保存記事（食品に制度免責文）で内容と不整合。冷凍保存の「一律 4 週間」は出典の裏付けが弱い。
- 重大度: B / 担当: ソネット5 / 並列可

### C 級（任意・代表のみ）

- トップ末尾「いま 0 本のツールを準備中です」の無意味表示、統計カード「0」の単体表示（Playwright 実測）
- 介護休業ツールは today 未指定時に失効判定をスキップ（初回レンダの一瞬のみ旧値。テストで仕様固定済み）
- `naha-okinawa.json` の命名規約違反（正: okinawa-naha）
- tests/hoikuryo.test.ts:57 の describe ラベル「87件」が実データ 147 件と乖離（検査自体は動的で健全）
- 件数ハードコードテスト 4 箇所（chomiryo:107 / josei-kenshin:170 / child-seat:119 / namonaki:16）
- gen-hoikuryo-municipalities.mjs が内容不変でも毎回 write し mtime 更新
- mascot/browser-generation.log にローカル Windows ユーザー名が露出
- 問い合わせ先が個人 Gmail（意図的公開・方針明示済み。是非は経営判断）
- モバイルヘッダーに「私たちについて」非表示（幅 0）・ハンバーガーなし
- fmtYen 等の金額フォーマット 40 箇所コピペ・共有 NumberField 不在（新規ツールから適用推奨）
- npm audit: moderate 2 件（next が依存する postcss。next のメジャー追随で解消する類。強制 fix は breaking のため放置が正解）
- LHCI の @lhci/cli バージョンフローティング、CI concurrency 未設定、engines 未宣言

## 2. 検証で「問題なし」を確認した項目（ゼロ報告）

- **全ゲート**: `npm run check`（lint/型/solves/brand/audience/mascot/vitest 71 ファイル 1,992 件）exit 0。フレッシュビルド（.next 削除後）成功・全 290 ページ生成
- **verify-seido --fetch**: 検査 184 ファイル / 出典 646 / 数値ノード 1,862 / 照合 3,049 文字列でエラー 0・警告 2（8/1 期限 12 日前の想定内警告のみ）
- **YMYL 独立検算 3 本**（一次資料から独立計算 → 本番実入力と突合。実装コードの式は不参照）:
  - 児童扶養手当: R8 確定額 48,050 円・逓減係数 0.0264029/0.0040719・10 円未満四捨五入・扶養 0 人の限度額 69 万/208 万まで**完全一致**（例: 所得 160 万・児童 1 → 双方 24,010 円/月）
  - 養育費: 表 1/表 2/表 3 の選択・按分指数 62/85 が裁判所公式 PDF と**一致**。年収から金額を算出しない（PDF 誘導に留める）設計は「捏造しない」原則として妥当
  - 扶養の壁: 実装 178 万/136 万は令和 8 年度税制改正準拠で**実装が正**（期待値側が令和 7 年分ルールで計算した見落とし。A-11 で再検算を起票)
- **8/1 D1 フェイルセーフ**: expiresOn=2026-07-31 → 2026-08-01 から停止を境界テスト込みで確認。停止対象は実は 5 ツール（産休育休・時短・復職手取り・介護休業・産後手続き）。停止文言・復帰シナリオテストも完備。**準備 OK**（残タスクは新額差し替えの実行のみ = A-1）
- **プライバシー**: トークン/秘密鍵/認証ファイルは作業ツリー・全 439 コミット履歴とも検出ゼロ。パーソナライズは localStorage 完結（アプリコードに fetch 自体が 0 件・API ルート 0 件）、SSR 非露出を本番 HTML で実測確認。/privacy の記載と実態一致
- **ブランド哲学**: アフィリエイト URL・広告タグ・PR 表記・寄付/課金/メルマガ導線は content 111 記事 + components 152 ファイル + 本番 HTML 全数スキャンで**検出ゼロ**。外部スクリプト 0 本（自前チャンクのみ）。/ads.txt 404
- **SEO 基盤**: robots.txt 健全・sitemap 184 URL がリポジトリ実体と完全一致・noindex 残存ゼロ・孤立ページ 0・og:image 6/6 生存・404 はハード 404
- **保育料データ品質**: nextCheckDue 欠落 0・期限切れ 0。サンプル精査 10 自治体中 9 は「未確認」がすべて一次情報の欠落を正直に注記したもので階層表本体は完備（例外は B-2 呉市）
- **記事正確性**: 型別 3 本ずつ抜き取り査読で明白な数値誤りゼロ（法定期限・給付率・R7 改正の反映など正確。むしろ模範的）

## 3. 性能実測値（Lighthouse mobile・本番・2026-07-19）

| ページ | Perf | LCP | CLS | TBT | 判定 |
|---|---|---|---|---|---|
| トップ / | 99 | 2.3s | 0 | 20ms | バジェット内 |
| 記事 /guide/childcare/dandori-hokatsu-tejun | 98 | 1.6s | **0.08** | 20ms | CLS がバジェット 0.05 超過 |
| ツール /tools/money/sankyu-ikukyu-money | **70** | **5.7s** | 0 | 390ms | LCP・Perf とも大幅超過 |
| ツール /tools/childcare/hoikuryo | **77** | **5.6s** | 0 | 160ms | 同上 |
| カテゴリ /tools/money | — | — | — | — | 404（ページ不存在 = B-8） |

補足: 素の遷移速度は速い（全ページ load 1.2 秒以内・TTFB ≤ 106ms を Playwright 実測）。LCP 悪化は初回訪問のバンドルダウンロード起因（S-2）。A11y 97〜100・BP 100・SEO 92〜100。

## 4. 次波割当案（ディスパッチ可能粒度）

### フェイブル5（設計判断・仕様曖昧さ解消・高リスク方針）

1. **F-1 デプロイドリフト原因究明と再発防止設計**（S-1）: 社長の Vercel ダッシュボード確認（D-1）と併走し、原因（連携切れ/失敗/手動運用)を特定。再発防止（デプロイ成功を機械確認する手段。例: ビルド ID をフッタ埋め込み + 日次照合）を設計し起票。完了条件: 本番 SHA = master SHA が機械的に確認できる状態。
2. **F-2 ツール探索性の情報設計**（A-5/A-6/B-8 統合）: カテゴリページ復活か /tools フィルタ強化かを決定し、パーソナライズを一覧に反映する仕様・検索の Enter 挙動仕様を書く。完了条件: specs/ に仕様書 1 本、実装タスクがオーパス/ソネットに分解済み。
3. **F-3 扶養の壁 178 万/136 万の独立再照合**（A-11): 財務省令和 8 年度大綱・国税庁あらましの原文と fuyou-kabe.json の全壁値（178/136/106/130/150/160/207 万）を突合。完了条件: 照合結果を BACKLOG に記録（乖離ゼロ or 修正 PR 起票)。
4. **F-4 児童扶養手当の年収→所得換算補助の仕様**（A-8): kyuyo-shotoku-koujo.json 流用で「年収からの概算モード」を安全に提供できるか判断（hoikuryo 8a の前例に倣う）。扶養親族数の初期値/自動連動の仕様も決める。完了条件: 仕様書 or「やらない」判断の記録。
5. **F-5 マスコット権利方針**（B-6): 現状維持/API 再生成/権利記録整備の 3 案比較と社長への意思決定資料。完了条件: docs/09 への方針追記案。
6. **F-6 8/1 D1 の独立検算役**（A-1): docs/10 §1.6 の台本どおり。完了条件: 新額の照合 PASS と公開判定。

### オーパス4.8（重実装・独立検算・インフラ)

1. **O-1 ツールバンドル分割**（S-2）: page.tsx の 64 静的 import を slug 単位の dynamic import へ、formula 解説を各ツール隣へ分離、保育料 147 自治体 JSON の遅延ロード化。完了条件: `/tools/[category]/[slug]` の First Load JS を 200kB 級へ・本番相当ビルドで hoikuryo/sankyu の LCP ≤ 2.5s（ローカル LHCI 実測）・全テスト green。
2. **O-2 8/1 D1 差し替え実行**（A-1）: docs/10 §1.5-1.6 台本どおり。完了条件: verify-seido --fetch PASS・5 ツール再開・CI green。
3. **O-3 パーソナライズのハイドレーション競合修正**（A-3): 初回タップ空パネルの解消（ロード完了までプレースホルダ/disabled 等)。完了条件: Playwright で load 直後タップ→フォーム表示を確認。
4. **O-4 日付ユーティリティ統合**（A-12): lib/tools/date.ts へ一本化・15 ファイル置換。完了条件: 重複定義 0・全テスト green。
5. **O-5 CI 強化**（B-4）: check:mascot 追加・LHCI に hoikuryo と記事 URL を追加（O-1 完了後に実施。順序厳守）・CLS 0.08 の記事ページ改善。完了条件: CI が S-2/CLS 劣化を検出できる状態で green。
6. **O-6 セキュリティヘッダ**（B-5): next.config.ts headers() に XFO/nosniff/Referrer-Policy/Permissions-Policy（CSP は設計込みで別判断)。完了条件: 本番ヘッダ実測で付与確認。
7. **O-7 verify-seido 死角の解消**（B-3): 空 fees 検知・「未確認」件数の増分監視・under-review への再訪期限必須化。完了条件: 既存 184 ファイルがエラー 0 のまま新ゲートが有効。
8. **O-8 記事レンダラーのコードフェンス対応 or 禁止 lint**（A-9): 完了条件: 該当 5 記事が正常表示。

### ソネット5（量産・定型)

1. **N-1 リンク切れ 19 箇所の一括修正**（S-3): `/articles/` → `/guide/<category>/`、part-shift のカテゴリ修正。完了条件: 全 199 URL クロールで 404 = 0（サンプル 3 本除く）。
2. **N-2 文字化け修正**（A-4): DeferredPersonalization.tsx:37「姊活・姊娠」→「妊活・妊娠」。完了条件: 本番相当ビルドで表示確認。
3. **N-3 記事表示崩れ修正**（A-9): コードフェンス 5 記事の書き換え・「0.3rate」→「3 割」・「整備済みデータでは」等の内部用語除去。完了条件: check-fidelity PASS・本番相当で目視確認。
4. **N-4 保育料カードの市名羅列解消**（A-7): 「147 自治体対応（毎週拡大中)・お住まいの市を検索」形式へ。完了条件: トップ/一覧/ツールヒーローの 3 箇所とも羅列消滅。
5. **N-5 サンプル記事の非公開化**（A-13): 本番ビルドから除外。完了条件: 本番 404・開発では閲覧可。
6. **N-6 数字不整合の解消**（B-9(3)): 「48+ FREE TOOLS」を registry 由来の実数に。完了条件: トップと /tools の数字が同一ソースから導出。
7. **N-7 brand-lint 強化**（A-10): 禁止ドメイン/タグ/sponsored 属性の検知追加（P2-I06 統合)。完了条件: テスト用ダミー混入を CI が赤にする。
8. **N-8 canonical 6 ページ + 404 title**（B-7): 完了条件: 実 HTML で canonical 付与確認。
9. **N-9 GSC verification meta 追加**（A-2、社長のプロパティ作成後): 完了条件: GSC で所有権確認済み・sitemap 送信済み。
10. **N-10 呉市データの扱い変更**（B-2)・**R7 据置 13 自治体の再訪キュー化**（B-1)・**naha-okinawa リネーム**・**describe「87件」ラベル修正**・**制度型テンプレ見出しの条件分岐**（B-10)。完了条件: 各該当ファイルの是正と CI green。

並列可否の注意: O-1（page.tsx 大改修)は wo-codex（UI 格上げ)と、N-4/B-9 系は同じく wo-codex と衝突しうる。着手宣言を queue で共有してから開始すること。O-5 の LHCI 追加は O-1 完了が前提（順序逆だと CI が恒常赤)。

## 5. TOP10 優先課題（重大度 × 効果順)

1. **S-1 デプロイドリフト解消** — 並行 6 系統が積んだ 48 自治体分の成果がユーザーに 1 件も届いていない。全作業の価値を毀損中。
2. **S-2 ツールバンドル分割** — 本番 LCP 5.7s は「遅い」と感じる水準で、保育料量産を続けるほど自動的に悪化する構造欠陥。
3. **A-1/O-2/F-6 8/1 D1 実行態勢** — 12 日後に 5 ツール停止 + CI 恒常赤が確定するカレンダーデッドライン。
4. **S-3 リンク切れ 19 箇所** — 記事→ツールの中核導線が死んでおり、機械置換 1 PR で直る費用対効果最大の修正。
5. **A-2 GSC 登録** — インデックスされているかすら分からない状態では SEO 施策全体が計器なし飛行。
6. **A-6/F-2 /tools 探索性** — 「カテゴリ分かりづらい問題」は未解消と実測判定。64 本の資産が見つからなければ存在しないのと同じ。
7. **A-3/A-4 パーソナライズの初回空パネル + 文字化け** — 新目玉機能の第一接触が「壊れてる + 誤字」では信頼を売る戦略と矛盾。
8. **A-9/N-3 記事表示崩れ** — バッククォート生表示と内部用語露出は YMYL サイトの品質シグナルを直撃。
9. **A-7 保育料の市名羅列** — 最大の差別化資産（自治体データ)の見せ方が最も安っぽいという逆転を解消。
10. **A-10/B-3/B-4 ゲートの穴 3 点**（brand-lint 収益タグ・verify-seido null/未確認・CI check:mascot/LHCI) — 「機械が守る」体制の死角を塞ぎ、以後の量産の安全速度を上げる。

## 6. 社長確認事項

- **D-1 Vercel ダッシュボード**（S-1・最優先): 本番デプロイの最終成功時刻と対象コミット、自動デプロイ（Git 連携)の状態、失敗ログの有無。→ 結果次第で F-1 の設計が変わる。
- **D-2 GSC**: プロパティ登録の有無（リポジトリからは未登録に見える)。未登録なら URL プレフィックスで登録 → N-9 で verification meta を入れる。
- **D-3 独自ドメイン取得の意思とタイミング**（BACKLOG Q0-2 で「社長が別途取得」と決裁済み): vercel.app のままだと被リンク・ブランド検索・GSC ドメインプロパティで恒久的に不利。被リンクが付き始める前の移行が定石。
- **D-4 問い合わせ窓口**: 個人 Gmail 公開の継続可否（独自ドメイン取得なら info@ 系へ移行が自然)。
- **D-5 マスコット画像の権利方針**（B-6): F-5 の比較資料を受けての意思決定。
- **D-6 8/1 当日の体制確認**: docs/10 §1.6 の台本（オーパス = 差し替え / フェイブル = 検算・公開)を実行する 2 セッションの確保。

## 7. 診断メタ情報

- 診断基準 SHA: `957790ac431b0f0a929d014397509d8e5fa1dbcd`
- 診断中のドリフト: 診断期間中に origin/master への新規コミットなし（PR 作成直前に再確認し、差異があれば本節に追記する)
- 実測成果物（セッション scratchpad・非コミット): Lighthouse JSON 5 本 / Playwright スクリーンショット約 80 枚 / クロール結果 crawl_results.json / データ全数集計 census-out.json / YMYL 突合記録
- 本診断の限界: GSC・Vercel・Analytics の管理画面は権限外で未確認（D-1/D-2)。インデックス状況の site: 検索は検索経路の精度保証なし。記事査読は 111 本中 9 本の抜き取り。Lighthouse は各 URL 1 回計測（CI は 3 回中央値)。
